"""Workflow system — CC-style multi-agent orchestration.

Primitives: agent(), parallel(), pipeline(), phase(), log()
Engine: WorkflowEngine — spawns sub-agents, manages scheduling
Registry: WorkflowRegistry — register, discover, run workflows
"""
