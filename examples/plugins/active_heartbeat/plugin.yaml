schema_version: 1
key: examples/active-heartbeat
name: Active Heartbeat Example
version: "0.1.0"
plugin_api: ">=1,<2"
description: Minimal Gateway-owned active runner with revisioned storage.
kind: general
entrypoint: active_heartbeat:register
requires:
  sdk: ">=0.1,<1"
provides: [active]
tags: [example, active]
enabled_by_default: false
