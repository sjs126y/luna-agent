schema_version: 1
key: integrations/github-assistant
name: GitHub Assistant
version: "0.2.0"
plugin_api: ">=1,<2"
description: GitHub MCP workflows for repository summaries, pull request review, issue triage, and release notes.
kind: integration
entrypoint: github_assistant:register
requires:
  sdk: ">=0.1,<1"
requires_env: [GITHUB_MCP_AUTH]
provides: [mcp, skill, command, hook, active]
tags: [github, mcp, development, review, active]
enabled_by_default: false
source: local
