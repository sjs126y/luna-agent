schema_version: 1
key: integrations/codex-bridge
name: Codex Bridge
version: "0.1.0"
plugin_api: ">=1,<2"
description: Connect Luna Agent to Codex MCP and App Server for isolated, asynchronous plugin development sessions.
kind: integration
entrypoint: codex_bridge:register
requires:
  sdk: ">=0.1,<1"
provides: [mcp, hook, tool, skill, active]
tags: [codex, mcp, app-server, plugin-development, coding]
enabled_by_default: false
source: local
