schema_version: 1
key: integrations/codex-bridge
name: Codex Bridge
version: "0.1.0"
plugin_api: ">=1,<2"
description: Register the official Codex MCP server with Lumora-enforced session boundaries.
kind: integration
entrypoint: codex_bridge:register
requires:
  sdk: ">=0.1,<1"
provides: [mcp, hook]
tags: [codex, mcp, coding]
enabled_by_default: false
source: local
