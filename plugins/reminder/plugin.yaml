schema_version: 1
key: automation/reminder
name: Reminder
version: "0.1.0"
plugin_api: ">=1,<2"
description: Persistent reminders delivered through Luna Agent's conversation runtime.
kind: automation
entrypoint: reminder:register
requires:
  sdk: ">=0.1,<1"
  python: ["pydantic>=2.8,<3"]
provides: [active, tools, command]
tags: [active, reminder, scheduling, productivity]
enabled_by_default: false
source: local
